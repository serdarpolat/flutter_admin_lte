import 'package:flutter/material.dart';

class MailboxBase extends StatelessWidget {
  const MailboxBase({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
