import 'package:flutter/material.dart';

class ExtrasBase extends StatelessWidget {
  const ExtrasBase({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
