import 'package:flutter/material.dart';

class FormsBase extends StatelessWidget {
  const FormsBase({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
