import 'package:flutter/material.dart';

class Validations extends StatelessWidget {
  const Validations({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
