import 'package:flutter/material.dart';

class GeneralElements extends StatelessWidget {
  const GeneralElements({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
