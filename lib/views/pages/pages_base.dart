import 'package:flutter/material.dart';

class PagesBase extends StatelessWidget {
  const PagesBase({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
