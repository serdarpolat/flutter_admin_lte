import 'package:flutter/material.dart';

class GalleryBase extends StatelessWidget {
  const GalleryBase({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
