import 'package:flutter/material.dart';

class TabbedIFramePluginBase extends StatelessWidget {
  const TabbedIFramePluginBase({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
