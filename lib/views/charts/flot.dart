import 'package:flutter/material.dart';

class Flot extends StatelessWidget {
  const Flot({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
