import 'package:flutter/material.dart';

class ChartsBase extends StatelessWidget {
  const ChartsBase({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
