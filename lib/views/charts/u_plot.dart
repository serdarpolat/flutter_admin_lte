import 'package:flutter/material.dart';

class UPlot extends StatelessWidget {
  const UPlot({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
