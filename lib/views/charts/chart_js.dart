import 'package:flutter/material.dart';

class ChartJS extends StatelessWidget {
  const ChartJS({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
