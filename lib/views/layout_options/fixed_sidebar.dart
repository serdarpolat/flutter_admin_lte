import 'package:flutter/material.dart';

class FixedSidebar extends StatelessWidget {
  const FixedSidebar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
