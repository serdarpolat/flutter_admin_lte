import 'package:flutter/material.dart';

class LayoutOptionsBase extends StatelessWidget {
  const LayoutOptionsBase({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
