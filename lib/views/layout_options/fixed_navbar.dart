import 'package:flutter/material.dart';

class FixedNavbar extends StatelessWidget {
  const FixedNavbar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
