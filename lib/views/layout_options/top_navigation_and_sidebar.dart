import 'package:flutter/material.dart';

class TopNavigationAndSidebar extends StatelessWidget {
  const TopNavigationAndSidebar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
