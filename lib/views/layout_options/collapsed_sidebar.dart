import 'package:flutter/material.dart';

class CollapsedSidebar extends StatelessWidget {
  const CollapsedSidebar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
