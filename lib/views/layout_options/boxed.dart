import 'package:flutter/material.dart';

class Boxed extends StatelessWidget {
  const Boxed({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
