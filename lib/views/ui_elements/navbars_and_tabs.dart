import 'package:flutter/material.dart';

class NavbarsAndTabs extends StatelessWidget {
  const NavbarsAndTabs({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
