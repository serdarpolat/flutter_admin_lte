import 'package:flutter/material.dart';

class UIElementsBase extends StatelessWidget {
  const UIElementsBase({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
