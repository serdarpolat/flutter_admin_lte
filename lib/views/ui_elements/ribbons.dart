import 'package:flutter/material.dart';

class Ribbons extends StatelessWidget {
  const Ribbons({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
