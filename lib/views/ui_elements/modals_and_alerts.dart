import 'package:flutter/material.dart';

class ModalsAndAlerts extends StatelessWidget {
  const ModalsAndAlerts({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
