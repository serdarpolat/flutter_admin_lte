import 'package:flutter/material.dart';

class JSGrid extends StatelessWidget {
  const JSGrid({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
