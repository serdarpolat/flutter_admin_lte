import 'package:flutter/material.dart';

class TablesBase extends StatelessWidget {
  const TablesBase({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
